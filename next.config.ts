// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "skillicons.dev",
      "user-images.githubusercontent.com",
    ],
  },
};

module.exports = nextConfig;
